# Copy to keystore.properties (gitignored) and fill in. Path is relative to repo root.
# CI alternative: set RELEASE_STORE_FILE / RELEASE_STORE_PASSWORD / RELEASE_KEY_ALIAS / RELEASE_KEY_PASSWORD env vars instead.
storeFile=upload-keystore.jks
storePassword=
keyAlias=
keyPassword=
